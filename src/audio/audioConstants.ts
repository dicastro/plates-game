// Placeholder seed for Home/menu ambient music. No music-personalization design
// has been decided yet (see doc/technical/audio-engine.md §7 — those are open
// proposals, not a finalized spec). This constant exists only so the audio
// toggle has deterministic, real audio to play until that design is settled.
export const HOME_AMBIENT_SEED = 1;